#include <iostream>
#include <vector>

void mergesort(){
    int arr[] = {38, 27, 43, 3, 9, 82, 10};
    std::size_t len = sizeof(arr) / sizeof(arr[0]);
}
int main() {
    mergesort();
    return 0;
}

